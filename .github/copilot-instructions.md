# Copilot Instructions

## Project Guidelines
- RetroMesh is used for hands-on workshops, so changes must not make the library harder to consume or set up. Prefer non-breaking, additive API changes and zero-config defaults (e.g. embedded fallbacks) over changes that require consumer code or setup steps to change.